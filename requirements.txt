# AutoViral AI - 爆款视频分析系统
# 安装命令: pip install -r requirements.txt

# ── 界面框架 ──────────────────────────────
streamlit>=1.35.0

# ── AI 接口 ───────────────────────────────
openai>=1.30.0              # GPT-4o / 通义千问（兼容 OpenAI 协议）
google-generativeai>=0.7.0  # Gemini 1.5 Pro

# ── 视频/音频处理（核心）────────────────────
opencv-python>=4.9.0        # 视觉分析：镜头切换、运动检测
librosa>=0.10.1             # 音频分析：BGM 节拍、能量变化
moviepy>=1.0.3              # 视频基础操作（音频提取）
yt-dlp>=2024.11.18          # URL 视频下载（需定期更新以应对平台反爬）

# ── 数据处理 ──────────────────────────────
numpy>=1.26.0
pandas>=2.2.0

# ── 工具 ──────────────────────────────────
python-dotenv>=1.0.0        # 环境变量（可选）
browser-cookie3>=0.19.1     # 从浏览器提取 Cookie（解决抖音鉴权）

# ── 可选：ASR 语音转文字（强烈推荐安装，开启台词/方言分析）──
# pip install openai-whisper
# 效果：自动转写台词、识别方言、提取爆款金句、暗号梗
# 注意：首次运行会下载 ~150MB 的 base 模型
# openai-whisper>=20231117

# ── 可选：OCR 屏幕文字识别（推荐安装，开启字幕/贴纸分析）──
# pip install easyocr
# 效果：识别视频帧中的字幕、标题贴纸、屏幕文字
# 注意：首次运行会下载语言模型（~200MB）
# easyocr>=1.7.0
